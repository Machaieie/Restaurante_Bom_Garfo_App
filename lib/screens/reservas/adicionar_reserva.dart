import 'package:flutter/material.dart';

class AdicionarReserva extends StatelessWidget {
  const AdicionarReserva({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}